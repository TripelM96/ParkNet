@page
@model ParkNet.Pages.Floors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].LayoutRaw)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Floor[0].ParkingLot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Floor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LayoutRaw)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParkingLot.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
