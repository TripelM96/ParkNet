@page
@model ParkNet.Pages.ParkingLots.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ParkingLot</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <span class="text-danger">*</span>
                <label asp-for="ParkingLot.Name" class="control-label"></label>
                <input asp-for="ParkingLot.Name" class="form-control" />
                <span asp-validation-for="ParkingLot.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ParkingLot.Location" class="control-label"></label>
                <input asp-for="ParkingLot.Location" class="form-control" />
                <span asp-validation-for="ParkingLot.Location" class="text-danger"></span>
            </div>
           @*  <div class="form-group">
                <label for="SelectedParkingLotId">Parque</label>
                <select asp-for="SelectedParkingLotId" asp-items="Model.ParkingLots" class="form-control" id="parkingLotSelect">
                    <option value="">-- Escolha o parque --</option>
                </select>
            </div>
            <div class="form-group">
                <label for="SelectedFloorId">Piso</label>
                <select asp-for="SelectedFloorId" class="form-control" id="floorSelect">
                    <option value="">-- Escolha o piso --</option>
                </select>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
