@page
@model ParkNet.Pages.ParkingRates.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParkingRate[0].ParkingLot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParkingRate[0].MinutesGap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParkingRate[0].Preco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ParkingRate) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParkingLot.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinutesGap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
